let express,SERVER_PORT;_0dc‍.w("express",[["default",["express"],function(v){express=v}]]);_0dc‍.w("./config",[["SERVER_PORT",["SERVER_PORT"],function(v){SERVER_PORT=v}]]);



const app = express();
app.use(cors());

app.get('/', (req, res)=>{
    res.send('Working');
})

app.listen(SERVER_PORT, (err)=>{
    if(!err){
        console.log(`Server is listening on port ${SERVER_PORT}`);
    }
});
